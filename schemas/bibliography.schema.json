{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://ayter.com/langram/schemas/bibliography.schema.json",
  "title": "Bibliography",
  "description": "Every source the curriculum is allowed to cite. Content may not reference a key that is not here.",
  "type": "array",
  "minItems": 1,
  "items": {
    "type": "object",
    "required": ["key", "status", "claim"],
    "additionalProperties": false,
    "properties": {
      "key": { "type": "string", "pattern": "^[a-z0-9-]+$" },
      "status": {
        "enum": ["verified", "needs_citation"],
        "description": "needs_citation means the tradition is named but the reference has not been checked. Never fabricate one to make this say verified."
      },
      "claim": { "type": "string", "minLength": 10, "description": "What this source is being relied on for." },
      "authors": { "type": "string" },
      "year": { "type": "integer" },
      "title": { "type": "string" },
      "venue": { "type": "string" },
      "url": { "type": "string" },
      "area": { "enum": ["sla", "l2-speech", "memory", "turkish", "assessment"] }
    }
  }
}
